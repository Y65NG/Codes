import sys
animals = []
def changeToYear(pos, animal):
    if pos == 'previous':
        if animal == 'Dragon'

lines = sys.stdin.read().strip().split('\n')
N = int(lines[0])
names = {}
for i in range(1, N + 1):
    words = lines[i].split(' ')
    name,  = words[0]

